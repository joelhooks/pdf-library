---
"pdf-brain": patch
---

Update CLI branding and UX improvements

- Add ascii art banner to help output
- Add `--version` / `-v` flag
- Add `read` as alias for `get` command
- Rename all references from pdf-library to pdf-brain
